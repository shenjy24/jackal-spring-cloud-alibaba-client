server:
  port: 18081

management:
  endpoint:
    health:
      show-details: always
    web:
      exposure:
        include: "*"

spring:
  application:
    name: service-consumer
  cloud:
    nacos:
      server-addr: 127.0.0.1:18848